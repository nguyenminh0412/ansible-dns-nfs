;
; BIND reverse data file for local loopback interface
;
$TTL	604800
@	IN	SOA	{{ domain_name }}. root.{{ domain_name }}. (
			      1		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@	IN	NS	ns.
28.116	IN	PTR	{{ domain_name }}.
